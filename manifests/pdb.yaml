apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: air-app
  labels:
    app.kubernetes.io/name: air-app
    app.kubernetes.io/part-of: air-app-project
    kubernetes.azure.com/generator: devhub
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: air-app