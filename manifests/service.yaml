apiVersion: v1
kind: Service
metadata:
  name: air-app
  namespace: dev
  labels:
    app.kubernetes.io/name: air-app
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: air-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000